language: python

notifications:
  email: false

services:
  - postgresql

before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  
install:
  - cd application
  - pip install -r requirements.txt
  
  
script: python test.py

 
