language: python

services:
  - postgresql

before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  
install:
  - cd application
  - pip install SQLAlchemy
  - pip install Flask

  
  
script: python test.py

 
